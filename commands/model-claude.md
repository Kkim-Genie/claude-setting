---
description: 클로드 코드 기반 모델을 Claude로 변경
allowed-tools: Bash(cp)
---

# Model Claude 커맨드

클로드 코드의 기반 모델을 Claude(opus)로 설정한다.

cp ~/.claude/settings/settings-claude.json ~/.claude/settings.json
위 명령어 실행
